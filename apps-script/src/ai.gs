function classifyWithAI_(thread) {
  // Placeholder for later phases.
  // Intentionally left narrow: only ambiguous items should reach this function.
  return { action: 'review', label: CONFIG.labels.review, archive: false, reason: 'ai not implemented yet' };
}
